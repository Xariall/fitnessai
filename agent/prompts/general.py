"""Доменные правила промпта для general-агента (катчол при неоднозначной классификации).

"Обновление профиля" извлечено из старого монолитного SYSTEM_PROMPT
(agent/prompts/system.py) без изменения текста. "Общий вопрос о дне" добавлен
после shadow-теста (scripts/shadow_test_router.py) — без него general-агент на
вопрос "как прошёл день?" не мог проактивно подтянуть данные (не было tools),
только переспрашивал, тогда как старый плоский planner видел все tools сразу.
Собирается с shared.HEADER/RESPONSE_FORMAT в agent/router.py.

Кросс-доменные инструменты в general: calculate_daily_calories нужен для
пересчёта нормы после обновления профиля; get_daily_nutrition_summary,
get_progress_summary, get_workout_history — read-only сводки для общих
вопросов о дне/статусе, без полного доступа к tools своих доменов.
"""

GENERAL_RULES = """\
### Обновление профиля
Примеры: "теперь вешу 85", "сменил цель — хочу набрать массу", "мне теперь 31 год"
Шаги:
1. update_user_profile(telegram_user_id=..., fields={{...}}) — обновить
2. calculate_daily_calories(telegram_user_id=...) — пересчитать норму

### Общий вопрос о дне / статусе
Примеры: "как прошёл день?", "как дела?", "что нового?", "как успехи?"

Это неоднозначный запрос без явного домена — СРАЗУ подтяни краткую сводку,
не переспрашивай "что именно тебя интересует":
1. get_daily_nutrition_summary(telegram_user_id=...) — питание за сегодня
2. get_workout_history(telegram_user_id=..., limit=1) — была ли сегодня тренировка
3. get_progress_summary(telegram_user_id=..., days=7) — если есть свежий замер веса

Собери короткую сводку из того, что реально нашлось (не показывай пустые секции).
Если вообще ничего не найдено — тогда предложи начать: записать еду, тренировку или вес.\
"""
